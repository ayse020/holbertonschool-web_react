# Task 0 - Creating an interface for a student

This project creates a TypeScript interface `Student` with firstName, lastName, age, and location. 
Two student objects are created and rendered in a table using vanilla JavaScript.
